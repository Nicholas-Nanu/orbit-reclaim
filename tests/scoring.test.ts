// Phase 4: Reference-case scoring tests (CLAUDE.md §5.5).
// Placeholder until the scoring engine lands.
import { describe, it, expect } from "vitest";

describe("scaffold", () => {
  it("test harness runs", () => {
    expect(true).toBe(true);
  });
});
